[build]
  # Use npx to ensure turbo binary is found in the CI path
  command = "npx turbo run build --filter=web"
  publish = "apps/web/.next"

[build.environment]
  NODE_ENV = "production"
  # Ensures Netlify uses the correct Node version
  NODE_VERSION = "20"
