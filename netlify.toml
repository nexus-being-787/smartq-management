[build]
  # Build from the root so turbo can find and build workspace dependencies
  command = "npm run build --filter=web"
  publish = "apps/web/.next"

[build.environment]
  NODE_ENV = "production"
  # Ensures Netlify uses the correct Node version
  NODE_VERSION = "20"
